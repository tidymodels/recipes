% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/misc.R
\name{recipes_map_cols}
\alias{recipes_map_cols}
\title{Transform columns in place}
\usage{
recipes_map_cols(new_data, col_names, fn)
}
\arguments{
\item{new_data}{A tibble.}

\item{col_names}{A character vector, denoting columns to transform.}

\item{fn}{A function taking one to three arguments: the column to transform,
the position of that column within \code{col_names}, and its name. Only the
arguments that \code{fn} accepts are passed, so \verb{\\(x) x * 2}, \verb{\\(x, i) x - means[[i]]}, and \verb{\\(x, i, col_name) ...} are all valid. It must return a
vector the same length as the column it is given.}
}
\value{
\code{new_data} with \code{col_names} transformed by \code{fn}.
}
\description{
This helper function applies \code{fn} to each of \code{col_names} and assigns the
results back into \code{new_data} in bulk. It should be used in \verb{bake.step_*()}
functions instead of assigning to \code{new_data[[col_name]]} inside a \code{for} loop,
which is quadratic in the number of columns and becomes prohibitively slow
for wide data.
}
\details{
Use the position \code{i} to look up per-column values estimated during \code{prep()},
after aligning them to \code{col_names} once outside of \code{fn}. Do not use
\code{col_name} to index a named vector with one element per selected column, as
the repeated name matching is what makes the naive approach slow. \code{col_name}
is intended for error messages and for lookups into small objects.
}
\seealso{
\link{developer_functions}
}
\keyword{internal}
